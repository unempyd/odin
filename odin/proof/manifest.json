{
  "upstream": "539d4d1f32b4",
  "residuals": [
    {
      "id": "S",
      "title": "Agent status: host rows are never outranked by a client wall clock",
      "commit": "f6ce5d565f",
      "tests": ["src/renderer/src/runtime/web-session-tabs-sync-host-authority.test.ts"],
      "reproduce": "expected 'working' to be '(blocked|done)'"
    },
    {
      "id": "N",
      "title": "Per-attempt accounting: dispatched_at at the dispatch edge, exit_code recorded, wallclock derivable",
      "commit": "07184d77d6",
      "tests": [
        "src/main/runtime/orchestration/db/worker-dispatch/worker-dispatch-accounting.test.ts"
      ],
      "reproduce": "expected '[0-9 :-]+' to be null|expected undefined to be [17]"
    },
    {
      "id": "M",
      "title": "Injected (unsupervised) dispatches are reconciled on restart, never left live",
      "commit": "705005b5b3",
      "tests": [
        "src/main/runtime/orca-runtime-reconcile-legacy-worker-terminals-unsupervised-dispatch.test.ts"
      ],
      "reproduce": "expected 'dispatched' to be 'failed'",
      "note": "Runtime-level: reconcileLegacyWorkerTerminals() (the method orca serve, orcad and the desktop path share) now also reconciles injected dispatches; at upstream the dispatch stays 'dispatched'."
    },
    {
      "id": "K",
      "title": "Sub-agent identity is first-class, never a fabricated dispatch",
      "commit": "d7e67780a8",
      "tests": ["src/renderer/src/components/sidebar/worktree-subagent-child-rows.test.ts"],
      "reproduce": "expected undefined to deeply equal \\{ id: 'child-1'"
    },
    {
      "id": "I",
      "title": "Launch receipt effective comes from the installed CLI, never copied from requested",
      "commit": "cc23f1c460",
      "tests": [
        "src/main/runtime/rpc/methods/orchestration/worker/worker-launch-preferences.test.ts"
      ],
      "reproduce": "to deeply equal \\{ requested"
    },
    {
      "id": "I3",
      "title": "Launch receipt effective for Cursor also comes from the installed CLI, not the catalog-only clone",
      "commit": "odin(I3) -- see git log --grep I3",
      "tests": [
        "src/main/runtime/rpc/methods/orchestration/worker/worker-launch-preferences.test.ts"
      ],
      "reproduce": "\"source\": \"probe\"",
      "note": "Round-3 review finding: CURSOR_SESSION_OPTION_CATALOG sets supportsWorkerLaunchPreferences: true, but PROBEABLE_LAUNCH_AGENTS excluded cursor, so a cursor launch fell to the bottom catalog-only branch -- effective: {...selection}, honestly labelled source: 'catalog' but never checked against the installed CLI. At upstream this whole probe apparatus does not exist yet, so every agent's receipt is the unconditional clone; the reproduce regex pins the same 'source': 'catalog' (never 'probe') diff line the I entry's fix already closed for claude/codex."
    },
    {
      "id": "B",
      "title": "Worker liveness: an empty or non-enumerating host answer is unverifiable, never exited",
      "commit": "cfc277f2b1",
      "tests": ["src/main/runtime/orchestration/worker-terminal-process-liveness.test.ts"],
      "reproduce": "expected 'exited' to be 'unverifiable'"
    },
    {
      "id": "A",
      "title": "Retired-incarnation tombstone never mints a proven exit from an absent status",
      "commit": "731c49c558",
      "tests": ["src/main/daemon/terminal-host-process-inspection.test.ts"],
      "reproduce": "to match object \\{ verdict: 'unverifiable'"
    },
    {
      "id": "C",
      "title": "tui-idle waits never settle on silence; evidence is carried on the wire",
      "commit": "07522244e6",
      "tests": ["src/main/runtime/terminal-wait-name-only-idle.test.ts"],
      "reproduce": "expected \"vi\\.fn\\(\\)\" to be called with arguments",
      "note": "The wait-path test drives upstream's real terminal.wait on a quiesced name-only Codex title; the unit test for resolveTuiIdleVerdict (tui-idle-evidence.test.ts) cannot run upstream because the verdict function is new, so it is not used as the reproduction."
    },
    {
      "id": "H",
      "title": "Shipped launch defaults never bypass agent permissions; migration never injects bypass",
      "commit": "854b5956ba",
      "tests": [
        "src/shared/constants.test.ts",
        "src/main/persistence/applying-settings/terminal-settings-migrations.test.ts"
      ],
      "reproduce": "not to match /dangerously|to be ''"
    },
    {
      "id": "E",
      "title": "Managed hooks into other tools' configs require explicit opt-in (fail-closed)",
      "commit": "29624f65b9",
      "tests": ["src/main/agent-hooks/managed-agent-hook-controls.test.ts"],
      "reproduce": "expected 'install' to be 'skip'"
    },
    {
      "id": "F",
      "title": "Fresh installs are opted out of telemetry by default",
      "commit": "aacecbdaf4",
      "tests": ["src/main/persistence-cohort-and-identity-migration.test.ts"],
      "reproduce": "expected true to be false"
    },
    {
      "id": "G",
      "title": "Mirroring ~/.codex/auth.json into a runtime home requires consent; revocation clears it",
      "commit": "f14f8249f9",
      "tests": ["src/main/codex-accounts/runtime-home-system-default-mirror-readback.test.ts"],
      "reproduce": "expected true to be false"
    },
    {
      "id": "D",
      "title": "Runtime WS listener stays on loopback unless a live consent record exists; revocation re-closes",
      "commit": "6d10576a71",
      "tests": ["src/main/runtime/runtime-rpc-websocket-bind-host.test.ts"],
      "reproduce": "expected '0.0.0.0' to be '127.0.0.1'"
    },
    {
      "id": "show-contact-loss",
      "title": "terminal show forces connected/writable false and an unverifiable liveness verdict while the owning SSH target's relay is lost or reconnecting, instead of reading a stale connected:true",
      "commit": "odin(show-contact-loss) -- see git log --grep show-contact-loss",
      "tests": ["src/main/runtime/show-contact-loss.test.ts"],
      "reproduce": "expected true to be false",
      "note": "Found against a real host (odin/proofs/ssh-boundary.2026-09-15T01-39-52-596Z.json phase4): after the transport was iptables-dropped, `[ssh] Relay channel ... lost; reconnect attempt 1/6` and `Keepalive timeout` fired, yet `terminal show` kept answering connected:true/writable:true with no exitCause for all 17 samples over 90s. buildPtyTerminalSummary/buildTerminalSummary (src/main/runtime/orca-runtime-build-pty-terminal-summary.ts, orca-runtime-write-orchestration-pointer-pty.ts) read pty.connected straight off the record, which only a PTY data/exit event or an inventory sweep updates -- never the relay-lost/reconnect callbacks in src/main/ipc/ssh-relay-session-callbacks.ts. Fix reuses the existing SSH connection-state registry (getRegisteredSshState) rather than adding a second tracker: while the owning target's status is not 'connected', connected/writable are forced false and a new RuntimeTerminalSummary.liveness field reports {status:'unverifiable', reason:'reconnecting'|'transport_lost'} -- never exitCause/exited. A genuine owner-proven exit (pty.lastExitCause already set) is never overridden."
    }
  ],
  "closedByConstruction": [
    {
      "id": "A-relay",
      "title": "Relay tombstone applies the same proof gate (record-torn-down is not a death)",
      "commit": "013965c537",
      "tests": ["src/shared/retired-pty-incarnations.test.ts"],
      "note": "Closed by construction, not mechanically reproduced: upstream src/relay/pty-handler.ts:2412 hard-codes code: 0 in reapExitedPty even for evidence 'record-torn-down' (its own docblock at :2395-2402 says that is not a death), and :2632-2656 publishes verdict 'exited' from it. Odin routes the code through resolveRetiredPtyIncarnationCode (UNVERIFIED_PROCESS_EXIT_CODE unless the evidence is a proven exit) and the consumer applies the same isProvenProcessExit gate as the daemon (A). The race has no deterministic harness in the relay test surface, so its test pins the extracted mapping rather than reproducing the upstream path."
    }
  ]
}
